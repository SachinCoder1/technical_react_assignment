/**
 * @docs to not use the keys directly everywhere but instead we should use this so that we do not make spelling mistake anywhere and promote consistency
 */
export const AUTH_KEYS = {
    authToken: "auth_token",
}